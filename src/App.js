import React, { Component } from 'react';
import { Subscription } from 'react-apollo';
import gql from "graphql-tag";

class App extends Component {
    render() {
        return (
            <div>Super nice app!</div>
        );
    }
}

export default App;
